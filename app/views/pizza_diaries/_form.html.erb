<%= form_with(model: pizza_diary, local: true) do |form| %>
  <% if pizza_diary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pizza_diary.errors.count, "error") %> prohibited this pizza_diary from being saved:</h2>

      <ul>
      <% pizza_diary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :toppings %>
    <%= form.text_field :toppings %>
  </div>

  <div class="field">
    <%= form.label :thoughts %>
    <%= form.text_area :thoughts %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
